# تاریخچه تغییرات

تغییرات مهم پروژه در این فایل ثبت می‌شوند. قالب از اصول Keep a Changelog پیروی می‌کند و تا پیش از اولین انتشار پایدار، تغییرات در بخش «منتشرنشده» قرار می‌گیرند.

## منتشرنشده

### افزوده‌شده

- جابه‌جایی AJAX بین مراحل نصب وب همراه با وضعیت بارگذاری، مدیریت خطای شبکه و حفظ حالت بدون JavaScript
- شاخص درخواست‌های جدید و نرخ تبدیل بازدیدکننده به درخواست در داشبورد، همراه با تعریف مستند شاخص
- ترجمه دیتابیسی محتوای Carousel و راهنمای ارزیابی محصول، UX و الزامات اعتماد در فین‌تک
- راهنمای مرحله بعد از ارسال در فرم‌های عمومی و اعتبارسنجی شماره تلفن فارسی، عربی و بین‌المللی
- نصب‌کننده امن تحت وب و CLI با بررسی پیش‌نیاز، migration، ایجاد مدیر و lock file
- مدیریت صفحات پویا، SEO، رسانه، منو، FAQ و بخش‌های صفحه اصلی
- انتشار پیش‌نویس و زمان‌بندی‌شده و URLهای قابل مدیریت
- اعلان فرم‌های تماس، سفارش و دعوت به همکاری با وضعیت خوانده‌نشده
- پیشخوان قابل شخصی‌سازی و آمار تجمیعی بازدیدکنندگان
- گزارش فعالیت، خروجی داده، تنظیمات ایمیل، Cache، Maintenance و backup/restore
- رابط چندزبانه فارسی و انگلیسی با RTL/LTR و مسیرهای زبان‌دار
- تست‌های PHPUnit، تحلیل ایستا، coding style و CI نصب از صفر

### تغییرکرده

- رابط عمومی و پنل مدیریت با سطوح خنثی‌تر، تایپوگرافی فشرده، sidebar، header، فرم، جدول، کارت، dropdown، modal، badge و empty state یکپارچه و RTL-first بازطراحی شد.
- جایگاه ستون وضعیت درخواست‌های همکاری به پیش از ستون عملیات منتقل و چیدمان دکمه‌های عملیات در تمام جدول‌های مدیریت به یک ردیف افقی یکپارچه شد.
- عنوان و متن‌های «سفارش نرم‌افزار» به «درخواست خدمات» عمومی‌سازی و جدول مدیریت آن بدون اسکرول افقی بازطراحی شد.
- عملیات ویرایش و حذف بخش‌های صفحه اصلی به دکمه‌های صرفاً آیکونی و دسترس‌پذیر تغییر کرد.
- Hero و ناوبری عمومی برای اقدام واضح‌تر، کنترل دستی اسلاید، فوکوس صحیح و چیدمان بهتر موبایل بازطراحی شد.
- دریافت داده صفحه اصلی از View به کنترلر منتقل شد و ترجمه‌های مرتبط به‌صورت eager load دریافت می‌شوند.
- رابط عمومی و پنل با Tailwind CSS 4، daisyUI 5 و Design System داخلی بازطراحی شد.
- ساختار مدیریت در module مستقل `frontend/modules/admin` یکپارچه شد.
- ساختار دیتابیس به migrationهای `utf8mb4` و نام‌گذاری `snake_case` منتقل شد.
- ارسال ایمیل از SwiftMailer متروک به Symfony Mailer رسمی Yii منتقل شد.
- تشخیص PHP CLI در Installer وب برای Apache ویندوز اصلاح و اجرای migration در برابر قفل‌شدن pipe مقاوم شد.

### حذف‌شده

- وابستگی نصب به dump قدیمی دیتابیس
- بخش backend قدیمی و assetها و وابستگی‌های بدون استفاده

## روش ثبت نسخه بعدی

هنگام انتشار، موارد بخش منتشرنشده را زیر عنوان نسخه و تاریخ انتقال دهید و بخش منتشرنشده تازه‌ای بسازید. تغییرات شکستن سازگاری و مراحل ارتقا باید به‌طور مشخص ذکر شوند.
